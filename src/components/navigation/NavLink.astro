---
interface Props {
  href: string;
  current?: boolean;
}

const { href, current = false } = Astro.props;
---
<a 
  href={href} 
  class:list={[
    "text-neutral hover:text-primary transition px-4 py-2",
    current && "font-semibold text-primary"
  ]}
>
  <slot />
</a>